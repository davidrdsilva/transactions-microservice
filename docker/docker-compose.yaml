services:
    transactions-database:
        image: postgres:alpine
        container_name: transactions-database
        volumes:
            - transactions-database-data:/var/lib/postgresql/data
        env_file:
            - .env
        ports:
            - '5432:5432'
        networks:
            - transactions-network

networks:
    transactions-network:
        driver: bridge

volumes:
    transactions-database-data:
